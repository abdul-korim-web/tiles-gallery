
import React from 'react';
export const metadata = {
  title: 'Register | TilesHub',
  description: 'This is a TilesHub register page',
}
const Layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
}

export default Layout;
