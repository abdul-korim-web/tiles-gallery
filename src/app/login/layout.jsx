
import React from 'react';
export const metadata = {
  title: 'Login | TilesHub',
  description: 'This is a TilesHub login page',
}
const Layout = ({children}) => {
    return (
        <div>
            {children}
        </div>
    );
}

export default Layout;
